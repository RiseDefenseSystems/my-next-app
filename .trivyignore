# .trivyignore — suppress scanner warnings for auto-generated build artifacts
#
# .next/dev/build/Dockerfile is generated by the Next.js dev server on every
# `next dev` run. It is already gitignored (/.next/ in .gitignore) and is
# NOT part of the production Docker build. Warnings on it are false positives.
.next/
